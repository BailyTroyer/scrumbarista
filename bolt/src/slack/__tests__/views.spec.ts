describe("The views handlers", () => {
  describe("checkinView", () => {
    it("posts a message with a new standup", async () => {
      // validate message posted

      // validate API was called
    })
    it("updates a pre-existing standup", async () => {
      // validate message updated

      // validate API was called
    })
  })
})